name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: ingestx
          POSTGRES_PASSWORD: ingestx
          POSTGRES_DB: ingestx
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ingestx"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql+psycopg2://ingestx:ingestx@localhost:5432/ingestx
      REDIS_URL: redis://localhost:6379/0
      WEBHOOK_SECRET: whsec_ci_secret
      WS_AUTH_TOKEN: ci-stream-token

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check app workers tests

      - name: Run tests
        run: pytest tests/ -v --cov=app --cov=workers --cov-report=term-missing

  dashboard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: dashboard/package.json

      - name: Install and build dashboard
        working-directory: dashboard
        run: |
          npm install
          npm run build
